import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ryltsov.alex.codemods.issue',
  appName: 'ionic-latest-with-modules',
  webDir: 'www'
};

export default config;
